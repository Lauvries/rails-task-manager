<h1>Tasks: </h1>

<% @tasks.each do |task| %>
  <h2><%= link_to task.name, task_path(task) %></h2>
  <p>The details: <%= task.details %></p>
  <p>Done? <%= task.done %></p>
<% end %>

<h3><%= link_to 'Create new Task', new_task_path(@task) %></h3>
